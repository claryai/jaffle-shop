# Clary configuration file
projects:
  # The name of the project
  - name: jaffle_shop
    data_source: jaffle_shop
    database: postgres
    schema: test

    include:
      - "models/**/*.yml"
    exclude:
      - "models/staging/__sources.yml"